<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.dietAssistant.mapper.PlanFoodMapper">
    <insert id="add">
        insert into planfoods(plan_id, food_id, food_name, target_amount, unit, eat_time)
        VALUES (#{planId}, #{foodId}, #{foodName}, #{targetAmount}, #{unit}, #{eatTime})
    </insert>
</mapper>